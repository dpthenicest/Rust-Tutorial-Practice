fn main() {
    // Arrays
    let array = [1u32, 2, 3];
    let first_element = array[0]; // Index Notations in arrays

    // Tuples
    let tuple: (u32, i64, bool) = (1u32, 3, false);
    let first_element = tuple.0; // Dot Notation in tuples
    
    // Unit is an empty tuple
    let unit = ();
    
}
